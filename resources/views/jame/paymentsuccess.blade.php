@extends('layout')

@section('content')

<h1 class="text-center my-4 text-success">ชำระเงินเสร็จสิ้น</h1>

<!-- ข้อมูลตั๋วภาพยนตร์ -->
<div class="card mx-auto mt-4 shadow-lg" style="max-width: 600px; border-radius: 10px;">
    <div class="card-body bg-light text-dark p-5">
        <h2 class="card-title text-center mb-4 text-primary">🎬 ตั๋วภาพยนตร์ 🎬</h2>
        
        <!-- แสดงรายละเอียดของภาพยนตร์ -->
        <p><strong>🎥 ชื่อภาพยนตร์:</strong> {{ $selectedMovie->name }}</p>
        <p><strong>🗓️ วันฉาย:</strong> {{ \Carbon\Carbon::parse($selectedMovie->date)->format('d M Y') }}</p>
        <p><strong>⏰ เวลา:</strong> {{ \Carbon\Carbon::parse($selectedMovie->time)->format('H:i') }}</p>
        <p><strong>📍 สถานที่:</strong> {{ $selectedMovie->location }}</p>

        <!-- แสดงที่นั่งที่เลือก -->
        <p><strong>💺 ที่นั่งที่เลือก:</strong> {{ implode(', ', $selectedTickets) }}</p>

        <!-- ราคารวม -->
        <p><strong>💵 ราคารวมทั้งหมด:</strong> {{ $totalPrice }} บาท</p>
        
        <!-- ข้อมูลป๊อปคอร์นและเครื่องดื่ม ถ้ามี -->
        @if(!empty($popcornSummary))
        <p><strong>🍿 ป๊อปคอร์นและเครื่องดื่ม:</strong> {{ $popcornSummary }}</p>
        <p><strong>💰 ราคารวมป๊อปคอร์นและเครื่องดื่ม:</strong> {{ $pricepopcornSummary }} บาท</p>
        @endif

        <!-- ส่วนลด ถ้ามี -->
        @if(!empty($finalPrice))
        <p><strong>💸 ส่วนลดที่ได้รับ:</strong> {{ $discountAmount }} บาท</p>
        <p><strong>💳 ราคาหลังหักส่วนลด:</strong> {{ $finalPrice }} บาท</p>
        @endif
    </div>
</div>

<!-- หมายเหตุสำหรับการแคปหน้าจอ -->
<div class="text-center mt-4">
    <p class="alert alert-warning" style="font-size: 18px;">
        ⚠️ กรุณาแคปหน้าตั๋วหนังนี้เพื่อเป็นหลักฐานในการยืนยันสิทธิ์ที่โรงภาพยนตร์
    </p>
</div>

@endsection
